spring.datasource.url=jdbc:mysql://localhost:3306/zee5appjpa
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
# jpa's instructions --> dialect --> db type
spring.jpa.show-sql=true
# it will display all autogenerated sql queries
spring.jpa.hibernate.ddl-auto=create
spring.jpa.hibernate.naming.physical-strategy = com.zee.zee5app.utils.CustomNamingStrategy

#create: on every execution, it will create tables for u.
# on every exec, it will drop tables if exists , then it will create it.
#create-drop : for testing purpose. it will create tables, when closing, it will drop all tables created.
#update : 1) if nothing is in existance: it will create all tables
#		  2) if tables are in existance : it will compare your entity specifications with existing tables,,
#		if there is any mismatch then it will alter table but wont drop any column.
#validate: data jpa neither it will create nor modify the table.
#		it will use only for existing structures.
#none : assignment. 
